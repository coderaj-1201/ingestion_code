# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.29.0,<1.0

# ── Configuration and validation ──────────────────────────────────────────────
pydantic>=2.7.0,<3.0
pydantic-settings>=2.3.0,<3.0

# ── Azure AI Foundry / OpenAI ─────────────────────────────────────────────────
azure-ai-projects>=1.0.0b9
openai>=1.35.0,<2.0

# ── Azure Search ──────────────────────────────────────────────────────────────
azure-search-documents>=11.6.0b4,<12.0

# ── Azure Storage ─────────────────────────────────────────────────────────────
azure-storage-blob>=12.20.0,<13.0

# ── Azure Service Bus ─────────────────────────────────────────────────────────
azure-servicebus>=7.12.0,<8.0

# ── Azure Identity (Managed Identity in ACA, AzureCLI locally) ───────────────
azure-identity>=1.17.0,<2.0

# ── Azure Monitor (required — set APPLICATIONINSIGHTS_CONNECTION_STRING to activate)
# If you do not need telemetry, comment this out and configure_logging() will warn
# once at startup and continue without App Insights.
azure-monitor-opentelemetry>=1.6.0,<2.0

# ── Document parsing ──────────────────────────────────────────────────────────
pdfplumber>=0.11.0,<1.0
python-docx>=1.1.0,<2.0
openpyxl>=3.1.0,<4.0
python-pptx>=1.0.0,<2.0

# ── HTTP client (Graph API / manual ingest calls) ─────────────────────────────
httpx>=0.27.0,<1.0

# ── Retry logic for LLM calls ─────────────────────────────────────────────────
tenacity>=8.3.0,<10.0
